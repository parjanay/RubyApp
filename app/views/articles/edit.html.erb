<center>
<h1> Edit the article </h1>
<%= render 'form' %>
<%=link_to 'Cancel and Return to articles Listing', articles_path %>
<% if logged_in? %>
    <%=link_to 'LOGOUT NOW', root_path, method: :delete %> | 
<%else%>
     <%=link_to 'LOGIN NOW', login_path%> |
<%end%>
<center>